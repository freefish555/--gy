spring:
  datasource:
    url: jdbc:mysql://localhost:3306/djbh_system?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&sessionVariables=character_set_client=utf8mb4,character_set_connection=utf8mb4,character_set_results=utf8mb4
    username: root
    password: dev123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      connection-timeout: 30000
      max-lifetime: 1800000
      connection-init-sql: "SET NAMES utf8mb4 COLLATE utf8mb4_general_ci"

logging:
  level:
    com.gydl.djbh: DEBUG
